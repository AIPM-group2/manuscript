# ManuScript
ManuScript is an AI tool, which helps you discover and fix formatting issues in your academic article draft.

## Running the code
To run the application locally, follow these steps:

## Running the code
To run the application locally, follow these steps:
```
npm install
npm run dev -- --open
```

## Code Structure
You can find the main page in `src/routes/+page.svelte`. The core logic of the application is implemented in `src/smarts.ts`.